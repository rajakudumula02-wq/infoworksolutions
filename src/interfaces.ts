// Service interfaces for the dental image score analysis pipeline
// These are also exported from src/types.ts for convenience

export type {
  ValidationService,
  ImageQualityChecker,
  AnalysisQueue,
  AIWorker,
  AIModelClient,
  StorageService,
  ScoreReportSerializer,
} from "./types.js";
